package custom.localize.Bstd;

import com.efuture.javaPos.Logic.DisplaySaleTicketBS;


public class Bstd_DisplaySaleTicketBS extends DisplaySaleTicketBS
{
	/* public boolean isGrantRedClear()
    {
        // 检查小票是否已经被红冲
    	/* if (!((Bstd_NetService)NetService.getDefault()).checkInvoiceHC(String.valueOf(salehead.fphm), salehead.syjh))
        {
            return false;
        }

        // 检查权限
        return super.isGrantRedClear();
    	
    }*/
}
